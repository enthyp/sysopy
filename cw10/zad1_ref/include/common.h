#ifndef COMMON_H
#define COMMON_H

// Miscellaneous.

#define MIN(a,b) ((a) < (b)) ? (a) : (b)
#define MAX(a,b) ((a) < (b)) ? (b) : (a)

#define UNIX_PATH_MAX 108                   // Max length of Unix socket name.

#endif // COMMON_H
